# GENERATED FILE — DO NOT EDIT MANUALLY.
# Generated by: python scripts/compose/render_exposure_overrides.py
# Source: configs/exposure_profiles.yaml profile: master_open
#
# To regenerate: python scripts/compose/render_exposure_overrides.py
# To verify no drift: python scripts/compose/render_exposure_overrides.py --check
#
# 전체 stack host-published. Gateway bypass, vLLM runtime direct access, Risk Adapter, Prometheus, DCGM, cAdvisor 직접 접근 가능. 정상 진단/운영 모드.
#
# EXPOSURE_AUDIENCE must be set when using this profile.
# Allowed values: local_only, private_lan, vpn, public
# Structured diagnostics: configs/exposure_profiles.yaml profiles.master_open.diagnostics

services:
  main-llm-vllm:
    ports:
      - ${MAIN_LLM_VLLM_BIND_ADDR:-0.0.0.0}:${MAIN_LLM_VLLM_PORT:-9401}:9401
  embedding-vllm:
    ports:
      - ${EMBEDDING_VLLM_BIND_ADDR:-0.0.0.0}:${EMBEDDING_VLLM_PORT:-9402}:9402
  embedding-ko-vllm:
    ports:
      - ${EMBEDDING_KO_VLLM_BIND_ADDR:-0.0.0.0}:${EMBEDDING_KO_VLLM_PORT:-9406}:9406
  risk-prompt-vllm:
    ports:
      - ${RISK_PROMPT_VLLM_BIND_ADDR:-0.0.0.0}:${RISK_PROMPT_VLLM_PORT:-9403}:9403
  risk-adapter:
    ports:
      - ${RISK_ADAPTER_BIND_ADDR:-0.0.0.0}:${RISK_ADAPTER_PORT:-9405}:9405
  prometheus:
    ports:
      - ${PROMETHEUS_BIND_ADDR:-0.0.0.0}:${PROMETHEUS_PORT:-9410}:9090
  dcgm-exporter:
    ports:
      - ${DCGM_EXPORTER_BIND_ADDR:-0.0.0.0}:${DCGM_EXPORTER_PORT:-9412}:9400
  cadvisor:
    ports:
      - ${CADVISOR_BIND_ADDR:-0.0.0.0}:${CADVISOR_PORT:-9413}:8080
